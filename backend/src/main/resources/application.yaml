server:
  port: 8080

spring:
  application:
    name: backEndSpring
  profiles:
    active: local

  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  jpa:
    show-sql: true

logging:
  level:
    root: INFO
  file:
    name: app.log